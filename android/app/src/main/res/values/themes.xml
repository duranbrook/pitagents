<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. Compose renders its own surface; this is just
         the window background used before setContent() runs. -->
    <style name="Theme.AutoShop" parent="android:Theme.Material.NoTitleBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
